# Заданные значения
a <- 6  # Большее основание
b <- 4  # Меньшее основание
alpha <- 45  # Угол α в градусах

# Переводим угол α из градусов в радианы
alpha_rad <- alpha * (pi / 180)

# Вычисление высоты трапеции
h <- b * tan(alpha_rad)

# Вычисление площади трапеции
S <- 0.5 * (a + b) * h

S
