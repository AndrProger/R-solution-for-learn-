# Заданные значения
m <- 8.1
w <- 4.2
x <- 10
y <- 4
n <- 13

# Вычисление a
a <- tan(n^2 / m)^2

# Вычисление c
c <- (exp(3 * m) + log(w))^(1/3)

# Вычисление d
d <- x^2 + y

# Вычисление q
q <- (c - a) / d

q
